# Vue with Firebase & Webpack

## Instalacion

```bash
npm install
```

## Build 

⚠️⚠️⚠️ Lastimosamente, por ahora se debe ejecutar cada vez, que se realiza un cambio.

Para generar el archivo `build.js`

```bash
npm run build
```

## Open file with the browser `/dist/index.html`
